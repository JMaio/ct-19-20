void f () {
    if ((a + b)) {}
    
    if (
        (a + b) || (c + d)
    ) {}

    // -1 && 1

    // - [ 1 && 1 ]

    // (a + b) || c
    // (a + b)
    // ( exp 
    // ( a + exp
    // ( a + b
    // ( a + b )
    // ( a + b ) ||
        // exp
        // ( exp ) ...
        // ( a + b ) ...


        // (a + (b && d) + (c || (c < 2))) {}

    // a + b + c     ||    c < 2


    // a + b + c     ||    (c < 2)


    // "  a  +  "
    // "(      )  +  b "
    // "(             ) || (  exp  ) "
    // "                ||  c  <  2  "
    // "((a + b) + c) || ( c < 2)"
    // (IDENT + IDENT) ||
}